name: phase2-sync-main

on:
  push:
    branches: [feat/messaging-agent-runtime-gateway]

permissions:
  contents: write

jobs:
  sync:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feat/messaging-agent-runtime-gateway
          fetch-depth: 0
      - name: Merge current main into phase2 branch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git fetch origin main
          git merge origin/main --no-edit
          git push origin HEAD:feat/messaging-agent-runtime-gateway
